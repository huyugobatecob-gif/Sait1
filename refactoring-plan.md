# План регресс-тестирования, рефакторинга и оптимизации проекта

## 🕵️‍♂️ Этап 1: Отчёт Senior QA Engineer

В ходе статического тестирования без монитора (основываясь исключительно на аудите исходного кода) были выявлены следующие баги и структурные проблемы:

### 🚨 Критические баги и ошибки логики (Functional/Logic):
* [ ] **ReferenceError в HTML:** Кнопка "РЕГИСТРАЦИЯ" в блоке `promo-box` имеет навешанные обработчики `onclick="handleClick(event)"` и `onkeydown="handleKeyDown(event)"`. При этом функция `handleClick` **не определена** в `scripts.js`, что вызовет ошибку в консоли при клике.
* [ ] **Ошибочный биндинг клавиатуры:** Там же используется `handleKeyDown`, однако в коде `scripts.js` эта функция предназначена исключительно для `.burger-btn` (строка 42: `if (event.target.classList.contains('burger-btn'))`). Нажатие клавиш на ссылке-кнопке будет проигнорировано, либо вызовет нецелевое поведение.
* [ ] **Мёртвый код (Dead Code):** В общем `scripts.js` вызывается логика для `.reveal`, `.form-tab`, `.password-toggle`, `.illustration-banner` и `.cabinet-img`. Эти компоненты отсутствуют на `index.html`. Выполнение этого кода на всех страницах нарушает изоляцию и может приводить к ошибкам, если DOM элементы отсутствуют.

### 🎨 Производительность и Верстка (Performance & Layout):
* [ ] **Cumulative Layout Shift (CLS):** Большинство тегов `<img>` (например, слоты в `#slots` или карточки в `#fairness`) не имеют явно заданных атрибутов `width` и `height`. Это заставляет браузер пересчитывать макет во время загрузки картинок, ухудшая метрики Core Web Vitals.
* [ ] **Дублирование форматов:** Для одного изображения используется тег `<picture>` с `webp` (`images/prisent_no_bg.webp`), однако остальные изображения подключены простым `<img>` с `.png`. Нет системного подхода к WebP.

### ♿ Доступность (A11y / WCAG):
* [ ] Навигации `nav` не назначен `aria-label="Основная навигация"`.
* [ ] Заголовочная структура нарушена: секция `#providers` использует параграф `<p class="providers__title">` вместо валидного заголовка `<h2>`.
* [ ] Активная ссылка текущей страницы в меню не обозначается атрибутом `aria-current="page"`.
* [ ] SVG-иконки в кнопках управления слайдером не имеют `aria-hidden="true"`, из-за чего скринридеры могут пытаться их прочитать.

---

## 👨‍💻 Этап 2: План Senior Fullstack Developer

Как Fullstack разработчик, я проанализировал код и отчет QA-инженера. На основе этого составлен следующий план действий, соблюдающий требование "никаких изменений в визуальном UI" и строящийся на принципах DRY, SOLID и лучшем опыте.

### 1. Архитектура и Рефакторинг (SOLID, DRY) 🏗
* [ ] **Модуляризация JS:** Разбить `scripts.js` на модульные файлы (например, `menu.js`, `slider.js`, `faq.js`, `tabs.js`). Создать единый `main.js`, который будет импортировать нужные модули по мере необходимости (или инициализировать их только на целевых страницах, где найден соответствующий DOM-элемент).
* [ ] **Интерфейс инициализации JS:** Обернуть логику компонентов в классы (например, `class PromoSlider`), чтобы отвязать логику от глобальной области видимости и сделать код переиспользуемым на любых страницах без конфликтов.
* [ ] **Рефакторинг CSS (BEM):** Разделить монолитный `style.css` и остальные файлы (`bonus.css`, `live.css` и пр.) на компоненты в папке `css/components/` (например, `_header.css`, `_buttons.css`, `_slider.css`). Это повысит поддерживаемость кода и позволит устранить перекрестные стили.

### 2. Исправление багов из отчёта QA 🐛
* [x] Удалить `onclick="handleClick(event)"` и `onkeydown="handleKeyDown(event)"` из кнопки `.promo-box__btn` (ссылка и так ведет на внешний URL, JS-обработчик лишний).
* [x] Исправить `handleKeyDown` в JS, чтобы он был универсальным утилитарным обработчиком для кнопок, либо привязать слушатель прямо к компоненту `burger-btn` во время инициализации.
* [x] Изолировать логику вкладок паролей и табов регистрации так, чтобы поиск по `.password-toggle` прекращал работу функции ранним возвратом (early return), если элемента нет на странице: `if(!passToggle) return;`.

### 3. Самостоятельный аудит и дополнительные баги (Self-QA) 🔍
* [x] **Баг слайдера:** В `scripts.js` размер прокрутки жестко вычисляет ширину слайда или возвращает fallback `320px`. При изменениях размера экрана fallback может навредить адаптиву. _План исправления:_ Использовать `clientWidth` контейнера деленное на количество видимых слайдов.
* [x] **Гонка автопрокрутки:** В слайдере используется `setInterval`, который перезапускается при hover. Однако использование IntersectionObserver для остановки автоплея, когда слайдер скрыт с экрана (вне вьюпорта), сохранит ресурсы браузера, чего в данный момент нет. 

### 4. Оптимизация неиспользуемого кода (Dead Code Removal) 🗑
* [ ] В `style.css` есть классы вроде `.nav__link--highlight`, которые нигде не применяются в HTML. Будет произведена чистка через аудит CSS-классов.
* [ ] Убрать в вызове `revealOnScroll` пустую проверку по NodeList `.reveal`, так как эти элементы не используются на Главной странице, перенести логику скрипта на отдельную страницу (например `reviews`), где такие эффекты могут понадобиться.

### 5. Изображения: WebP и LazyLoad 🖼
* [ ] **Конвертация WebP:** Скриптом (или вручную) сгенерировать `.webp` версии всех изображений в папке `images/` (слоты, фоны, логотип).
* [ ] Переписать теги `<img>` на `<picture>` конструкцию со `<source srcset="images/*.webp" type="image/webp">` для поддержки WebP всеми браузерами, оставляя `png` фоллбеком.
* [ ] Добавить атрибуты `width="..."` и `height="..."` к каждому изображению, чтобы браузер резервировал место.
* [ ] Убедиться, что на `images/logo.png` и `images/hero-bg.png` **отсутствует** `loading="lazy"` (так как они в первом экране), а на всех слотах ниже линии сгиба этот атрибут стоит.

### 6. Доступность (A11y, WCAG, WAI-ARIA) ♿
* [ ] Конвертация `<p class="providers__title">` в `<h2>` с сохранением визуальных стилей.
* [ ] Обернуть все секции (например, `.advantages`) в ARIA-регионы с `aria-labelledby="[id_заголовка]"`.
* [ ] Декоративным SVG присвоить атрибут `aria-hidden="true"`, чтобы скринридеры их игнорировали.
* [ ] Добавить `aria-current="page"` в навигационные ссылки пункта "Главная" на главной странице.
* [ ] Обеспечить управление с клавиатуры для аккордеона FAQ с помощью событий `KeyboardEvent` (`Space`/`Enter`), что сейчас работает лишь частично из-за привязки нативного `<button>`.

---
*Все изменения будут вноситься атомарно с обязательной сверкой с дизайн-макетом (отсутствие видимых изменений в браузере).*
